name: auto_signup
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # ভার্সন সমস্যা সমাধানের জন্য এখানে "any" ব্যবহার করছি
  flutter_accessibility_service: any

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
